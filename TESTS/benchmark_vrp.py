[{'dimension': 0, 'capacity': 0, 'nodes': {}, 'demands': {}, 'try': 'with open(filepath', 'r': "as f:\n            lines = f.readlines()\n        \n        section = None\n        \n        for line in lines:\n            line = line.strip()\n            if not line or line.startswith('COMMENT'):\n                continue\n                \n            if line.startswith('DIMENSION'):\n                data['dimension'] = int(line.split(':')[1].strip())\n            elif line.startswith('CAPACITY'):\n                data['capacity'] = int(line.split(':')[1].strip())\n            elif line.startswith('NODE_COORD_SECTION'):\n                section = 'NODE_COORD'\n                continue\n            elif line.startswith('DEMAND_SECTION'):\n                section = 'DEMAND'\n                continue\n            elif line.startswith('DEPOT_SECTION'):\n                section = 'DEPOT'\n                continue\n            elif line.startswith('EOF'):\n                break\n                \n            # Process data based on current section\n            if section == 'NODE_COORD' and line and not line.startswith('NODE_COORD_SECTION'):\n                parts = line.split()\n                if len(parts) >= 3:\n                    node_id = int(parts[0])\n                    x = float(parts[1])\n                    y = float(parts[2])\n                    data['nodes'][node_id] = (x", "DEMAND' and line and not line.startswith('DEMAND_SECTION": "parts = line.split()\n                if len(parts) >= 2:\n                    node_id = int(parts[0])\n                    demand = int(parts[1])\n                    data['demands'][node_id] = demand\n                    \n    except Exception as e:\n        print(f", 'filepath}': {'e}': 'return None\n        \n    return data\n\ndef parse_sol_file(filepath: str) -> float:', 'Parse a .sol file to extract the optimal solution value."': 'try:\n        with open(filepath', 'r': "as f:\n            lines = f.readlines()\n            \n        # Look for the line containing the cost value\n        for line in lines:\n            line = line.strip()\n            if line.startswith('Cost') or line.startswith('cost') or ':' in line:\n                # Try to extract numeric value\n                parts = line.split()\n                for part in parts:\n                    try:\n                        return float(part)\n                    except ValueError:\n                        continue\n            elif line.replace('.'", 'isdigit()': 'Line is just a number\n                return float(line)\n                \n        # If not found', 'lines': 'words = line.split()\n            for word in words:\n                try:\n                    return float(word)\n                except ValueError:\n                    continue\n                    \n    except Exception as e:\n        print(f', 'filepath}': {'e}': 'return None\n    \n    return None\n\ndef calculate_distance(node1: Tuple[float', 'node2': 'Tuple[float', 'float': '', 'nodes."': 'return math.sqrt((node1[0] - node2[0])**2 + (node1[1] - node2[1])**2)\n\ndef calculate_route_distance(route: List[int]', 'nodes': 'Dict[int', 'depot."': 'if len(route) < 2:\n        return 0.0\n    \n    total_distance = 0.0\n    \n    # Add distance from depot to first node\n    total_distance += calculate_distance(nodes[1]', 'calculate_total_distance(routes': 'List[List[int]]', 'routes."': 'optimized_routes = []\n    for route in routes:\n        optimized_route = two_opt_route(route', 'clarke_wright_savings(nodes': 'Dict[int', 'demands': 'Dict[int', 'capacity': 'int) -> List[List[int]]:', 'Implement Clarke-Wright Savings Algorithm for VRP."': 'depot = 1  # Assume depot is node 1\n    customers = [node for node in nodes.keys() if node != depot]\n    \n    # Initialize routes - one route per customer\n    routes = [[customer] for customer in customers]\n    \n    # Calculate savings\n    savings = []\n    for i in range(len(customers)):\n        for j in range(i+1', 'len(customers))': 'customer_i = customers[i]\n            customer_j = customers[j]\n            \n            # Calculate savings s_ij = d_1i + d_1j - d_ij\n            depot_to_i = calculate_distance(nodes[depot]', 'customer': 'i for i', 'savings': 'route_i_idx = route_map[customer_i]\n        route_j_idx = route_map[customer_j]\n        \n        # Skip if same route\n        if route_i_idx == route_j_idx:\n            continue\n            \n        route_i = routes[route_i_idx]\n        route_j = routes[route_j_idx]\n        \n        # Check capacity constraint\n        total_demand = sum(demands[node] for node in route_i) + sum(demands[node] for node in route_j)\n        if total_demand > capacity:\n            continue\n            \n        # Merge routes - we can merge in two ways: i->j or j->i\n        # For simplicity', 'new_route': 'route_map[node] = route_i_idx\n    \n    # Remove empty routes\n    routes = [route for route in routes if route]\n    \n    return routes\n\ndef two_opt_route(route: List[int]', 'List[int]': '', 'route."': 'if len(route) < 3:\n        return route\n        \n    best_route = route[:]\n    best_distance = calculate_route_distance(route', 'improved': 'improved = False\n        for i in range(1', 'len(route))': 'Create new route by swapping segments\n                new_route = route[:]\n                new_route[i:j+1] = reversed(new_route[i:j+1])\n                \n                new_distance = calculate_route_distance(new_route', 'best_distance': 'best_route = new_route[:]\n                    best_distance = new_distance\n                    route = new_route[:]\n                    improved = True\n                    \n    return best_route\n\ndef two_opt_algorithm(routes: List[List[int]]', 'List[List[int]]': '', 'find_vrp_sol_pairs(directory': 'str) -> List[Tuple[str', 'str]]': '', 'directory."': "files = os.listdir(directory)\n    vrp_files = [f for f in files if f.endswith('.vrp')]\n    sol_files = [f for f in files if f.endswith('.sol')]\n    \n    pairs = []\n    for vrp_file in vrp_files:\n        # Try to find corresponding .sol file\n        base_name = vrp_file.replace('.vrp'", '.sol': 'f sol_file in sol_files:\n            pairs.append((\n                os.path.join(directory', 'else': "Try without suffix\n            for sol_file in sol_files:\n                sol_base = sol_file.replace('.sol'", 'base_name.startswith(sol_base)': 'pairs.append((\n                        os.path.join(directory', 'run_benchmark(directory': 'str) -> pd.DataFrame:', 'Run benchmark on all VRP instances in a directory."': 'results = []\n    pairs = find_vrp_sol_pairs(directory)\n    \n    print(f', 'benchmark': 'for vrp_path', 'pairs': "instance_name = os.path.basename(vrp_path).replace('.vrp'", 'print(f': 'rocessing {instance_name'}, ')\n        \n        # Load problem data\n        vrp_data = parse_vrp_file(vrp_path)\n        if vrp_data is None:\n            print(f': 'ailed to parse VRP file: {vrp_path'}, 'None': 'print(f', 'file': {'sol_path}': "continue\n            \n        # Run Clarke-Wright algorithm\n        cw_start_time = time.time()\n        cw_routes = clarke_wright_savings(\n            vrp_data['nodes']", 'else': "opt_gap = 0.0\n            \n        total_time = cw_time + opt_time\n        \n        # Store results\n        results.append({\n            'Instance Name': instance_name", 'Optimal Cost': 'optimal_cost', 'CW Cost': 'cw_cost', 'CW Gap %': 'cw_gap', 'CW Time': 'cw_time', '2-Opt Cost': 'opt_cost', '2-Opt Gap %': 'opt_gap', 'Total Time': 'total_time'}, 'print(f': 'ptimal: {optimal_cost:.2f'}, {'cw_cost': 0.2, 'cw_gap': 0.2, ' +\n              f': 'Opt: {opt_cost:.2f'}, {'opt_gap': 0.2, 'Time': {'total_time': 0.4, 'f}s': 'Create DataFrame\n    df = pd.DataFrame(results)\n    return df\n\ndef main():', 'Main function to run the benchmark."': 'Directory containing VRP instances\n    data_directory =', 'print(f': 'ata directory: {data_directory'}, 'os.path.exists(data_directory)': 'print(f', 'Error': 'Directory {data_directory'}, {'print(': 'nOverall Statistics:', 'print(f': 'otal instances processed: {len(results_df)'}, {'].mean():.2f}%")\n    print(f"Average 2-Opt Gap: {results_df[\'2-Opt Gap %': '.', 'mean()': 0.2, 'print(f': "verage Total Time: {results_df['Total Time'].mean():.4f"}]