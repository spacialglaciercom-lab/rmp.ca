['int, Tuple[float, float]], \n    routes: List[List[int]], \n    depot: int = 1\n) -> List[RouteSegment]:\n    """Convert CVRP solution to RouteSegment for GPX export."""\n    # Implementation in backend/app/cvrp_gpx.py\n\n# 2. CPP Solutions (Chinese Postman Problem)\ndef cpp_solution_to_gpx_segments(\n    G: nx.Graph,  # NetworkX graph with node coordinates\n    route_nodes: list  # List of nodes in Eulerian circuit\n) -> List[RouteSegment]:\n    """Convert CPP solution to RouteSegment for GPX export."""\n    # Implementation in backend/app/optimize.py\n\n# Backend API Endpoints Supporting GPX Export\n\n## 1. Benchmark Script with GPX Export\n\nRun the benchmark script with GPX export:\n\n```bash\npython benchmark.py --gpx --gpx-dir ./gpx_exports ./data\n```\n\nFeatures:\n- Exports both Clarke-Wright and 2-Opt solutions to separate GPX files\n- One GPX file per problem instance (e.g., A-n32-k5_cw.gpx, A-n32-k5_2opt.gpx)\n- GPX structure includes:\n  - `<wpt>` elements for all customer locations and depot\n  - `<trk>` elements with `<trkseg>` and `<trkpt>` for route visualization\n  - Proper metadata and naming for each route\n\n## 2. API Endpoints with GPX Support\n\n### CPP Optimization Endpoint\n- **URL**: POST /api/optimize\n- **GPX Export**: Add `Accept: application/gpx+xml` header to request\n- **Response**: GPX 1.1 file with waypoints and track points\n\n### VRP Solver Endpoint\n- **URL**: POST /api/vrp/solve\n- **GPX Export**: Add `Accept: application/gpx+xml` header to request\n- **Response**: GPX 1.1 file with one track per vehicle route\n\n# Android Integration Architecture\n\nThe mobile-scaffold directory provides a complete architecture for consuming both API JSON and GPX files.\n\n## MVVM Architecture Components\n\n### 1. RouteUiState.kt\nData classes representing the UI state:\n- `RouteUiState`: Main state container with list of routes\n- `RouteDisplay`: Individual vehicle route with waypoints and track\n- `WaypointDisplay`: Named stops with coordinates\n- `LatLon`: Simple coordinate pair\n\n### 2. RouteViewModel.kt\nViewModel managing data loading:\n- `loadFromApi()`: Fetch from backend API\n- `loadFromGpx()`: Parse local GPX files\n- Exposes `StateFlow<RouteUiState>` for reactive UI updates\n\n### 3. RouteRepository.kt\nRepository interface for data sources:\n- `solve()`: API endpoint calls (implement with Retrofit)\n- `parseGpx()`: Local GPX parsing (implement with XML parser)\n\n### 4. RouteMapScreen.kt\nJetpack Compose UI implementation (skeleton):\n- Use `collectAsStateWithLifecycle` for efficient updates\n- Render map with polylines from `uiState.routes.track`\n- Render markers from `uiState.routes.waypoints`\n\n## Implementation Steps for Android Integration\n\n1. Copy the mobile-scaffold package into your Android app\n2. Add required dependencies to build.gradle:\n   - `androidx.lifecycle:lifecycle-viewmodel-compose`\n   - `androidx.lifecycle:lifecycle-runtime-compose`\n   - `kotlinx-parcelize` plugin\n   - Map library (e.g., osmdroid or Google Maps Compose)\n\n3. Implement RouteRepository:\n   - Retrofit for API calls to backend\n   - XML parser for GPX files\n\n4. Add GPX file handling in AndroidManifest.xml:\n   ```xml\n   <activity android:name=".GpxViewerActivity" android:exported="true">\n       <intent-filter>\n           <action android:name="android.intent.action.VIEW" />\n           <category android:name="android.intent.category.DEFAULT" />\n           <data android:mimeType="application/gpx+xml" />\n           <data android:scheme="file" />\n           <data android:scheme="content" />\n       </intent-filter>\n   </activity>\n   ```\n\n5. Handle GPX file intents in Activity:\n   ```kotlin\nclass GpxViewerActivity : ComponentActivity() {\n    override fun onCreate(savedInstanceState: Bundle?) {\n        super.onCreate(savedInstanceState)\n        \n        if (intent?.action == Intent.ACTION_VIEW) {\n            val uri = intent.data\n            if (uri != null) {\n                loadGpxFromUri(uri)\n            }\n        }\n    }\n    \n    private fun loadGpxFromUri(uri: Uri) {\n        val xml = contentResolver.openInputStream(uri)?.bufferedReader().use { it?.readText() }\n        if (xml != null) {\n            val viewModel = // get your view model\n            viewModel.loadFromGpx(xml)\n        }\n    }\n}\n   ```\n\nThis architecture ensures seamless handling of both online (API) and offline (GPX file) route data with a single UI implementation.']