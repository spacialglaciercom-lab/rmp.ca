diff --git a/src/client.js b/src/client.js
index 3f748a3239830a7e10826c8e1ad7264551c89da0..ec8f65076abb53dddbd90587596b00bd977c1fd6 100644
--- a/src/client.js
+++ b/src/client.js
@@ -27,19 +27,21 @@ class NgrokClient {
         return await this.internalApi[method](path, { json: options }).json();
       }
     } catch (error) {
+      const response = error.response;
+      const body = response?.body ?? error.body ?? String(error.message || error);
       let clientError;
       try {
-        const response = JSON.parse(error.response.body);
+        const parsed = typeof body === "string" ? JSON.parse(body) : body;
         clientError = new NgrokClientError(
-          response.msg,
-          error.response,
-          response
+          parsed?.msg ?? body,
+          response ?? {},
+          parsed ?? body
         );
       } catch (e) {
         clientError = new NgrokClientError(
-          error.response.body,
-          error.response,
-          error.response.body
+          typeof body === "string" ? body : (body?.msg ?? "Tunnel connection failed"),
+          response ?? {},
+          body
         );
       }
       throw clientError;
@@ -52,8 +54,13 @@ class NgrokClient {
         (response) => response.statusCode === 204
       );
     } catch (error) {
-      const response = JSON.parse(error.response.body);
-      throw new NgrokClientError(response.msg, error.response, response);
+      const body = error.response?.body ?? error.body ?? "{}";
+      const response = typeof body === "string" ? JSON.parse(body) : body;
+      throw new NgrokClientError(
+        response?.msg ?? "Tunnel request failed",
+        error.response ?? {},
+        response
+      );
     }
   }
 
