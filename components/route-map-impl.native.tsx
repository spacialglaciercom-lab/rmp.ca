/**
 * Platform resolver: native. Metro picks this on iOS/Android.
 * Uses react-native-maps (Apple Maps / Google Maps) on native.
 */
export { RouteMap, type RouteMapProps, type RouteMapRef } from "./route-map.native";
