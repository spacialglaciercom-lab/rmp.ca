#!/bin/sh
# Wrapper for the TrashRoute GeoJSON extract service.
# Sources /usr/local/etc/rmpca/extract.env for environment variables.

[ -f /usr/local/etc/rmpca/extract.env ] && . /usr/local/etc/rmpca/extract.env

export PORT="${PORT:-4000}"

cd /app/extract
exec /usr/local/bin/node server.js
